/*
 * OtpForgotPassword Messages
 *
 * This contains all the text for the OtpForgotPassword container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OtpForgotPassword';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OtpForgotPassword container!',
  },
});
