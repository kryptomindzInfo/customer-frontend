/*
 * BillPaymentsPage Messages
 *
 * This contains all the text for the BillPaymentsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BillPaymentsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BillPaymentsPage container!',
  },
});
