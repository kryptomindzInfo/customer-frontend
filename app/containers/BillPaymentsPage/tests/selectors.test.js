// import { selectBillPaymentsPageDomain } from '../selectors';

describe('selectBillPaymentsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
