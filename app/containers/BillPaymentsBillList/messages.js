/*
 * BillPaymentsBillList Messages
 *
 * This contains all the text for the BillPaymentsBillList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BillPaymentsBillList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BillPaymentsBillList container!',
  },
});
